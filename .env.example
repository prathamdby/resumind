# Better Auth Configuration
# URL for the Better Auth server that handles authentication
BETTER_AUTH_URL=http://localhost:3000

# Python Backend API Configuration
# URL for the Python backend API that handles resume analysis and storage
API_URL=http://localhost:8000
